global:
  scrape_interval: 1m  # Set the scrape interval to 1 minute

scrape_configs:
  - job_name: 'speedport_metrics'
    static_configs:
      - targets: ['speedport_metrics:8000']  # Target the speedport_metrics service
